using System;

namespace CRS
{
    public class SCRController //: IContainer
    {


        //dbGet();
        // getStudent(id);
        // addStudent(name, blazerName, major);
        // modStudent(id, property, value);
        // delStudent(id);

        // getSCourse(id);
        // addSCourse();
        // modSCourse(id, property, value);
        // delSCourse(id);

        // getRegister(id);
        // addRegister();
        // modRegister(id, property, value);
        // delRegister(id);

    }
}